# Copy to .env (not committed). Rarely changes.
EXPO_PUBLIC_GOOGLE_SHEETS_URL=https://script.google.com/macros/s/PASTE_ID/exec
# Fallbacks, used if the profile doesn't set them:
EXPO_PUBLIC_DATASET=default
EXPO_PUBLIC_GOOGLE_SHEETS_TAB=Jobs